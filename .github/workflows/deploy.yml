      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build     # Vite builds to dist/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist           # 👈 MUST be dist for Vite
